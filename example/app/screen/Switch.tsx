import React from 'react';
import { } from 'react-native';

/**
 * @file <Switch /> Example
 */
interface Props { }

const SwitchExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default SwitchExample;
