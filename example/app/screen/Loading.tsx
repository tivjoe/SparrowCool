import React from 'react';
import { } from 'react-native';

/**
 * @file <Loading /> Example
 */
interface Props { }

const LoadingExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default LoadingExample;
