import React from 'react';
import { } from 'react-native';

/**
 * @file <ScrollVaryNavbar /> Example
 */
interface Props { }

const ScrollVaryNavbarExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default ScrollVaryNavbarExample;
