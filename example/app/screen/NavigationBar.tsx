import React from 'react';
import { } from 'react-native';

/**
 * @file <NavigationBar /> Example
 */
interface Props { }

const NavigationBarExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default NavigationBarExample;
