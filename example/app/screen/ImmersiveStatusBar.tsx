import React from 'react';
import { } from 'react-native';

/**
 * @file <ImmersiveStatusBar /> Example
 */
interface Props { }

const ImmersiveStatusBarExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default ImmersiveStatusBarExample;
