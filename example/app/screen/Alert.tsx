import React from 'react';
import { } from 'react-native';

/**
 * @file <Alert /> Example
 */
interface Props { }

const AlertExample: React.FC<Props> = React.memo(({ }) => {
    return <></>
})

export default AlertExample;
